class suitcase{
    constructor (x,y,color){
        this.x = x;
        this.y = y;
        this.color = color;
    }
    suitcase (
        y=0
        rect(20, 50, random(0, windowWidth), y)
        y--
    )

}