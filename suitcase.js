class Pancake{
    constructor (color){
        this.x = random(0, windowWidth);
        this.y = windowHeight;
        this.color = color;
    }
    pancakeFall (){
        y=0
        rect(20, 50, y)
        this.y--
    }
    pancakeCatch (){
        if this.x == platePosition
        
    }


}